<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.5</version>
		<relativePath /> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.example.spring.ai</groupId>
	<artifactId>parallelization-workflow</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>parallelization-workflow</name>
	<description>Agentic Parallelization Workflow pattern with Spring AI, made durable on Diagrid Catalyst</description>
	<properties>
		<!-- Java 21: a durable ChatClient.call() blocks until its workflow completes, so the app
		     runs on virtual threads (spring.threads.virtual.enabled) to keep that wait cheap. -->
		<java.version>21</java.version>
		<spring-ai.version>2.0.0</spring-ai.version>
		<dapr.version>1.18.0</dapr.version>
		<diagrid-spring-ai.version>0.2.0</diagrid-spring-ai.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-openai</artifactId>
		</dependency>

		<!-- THE ONLY THING THAT MAKES IT DURABLE. Adding this starter turns every
		     ChatClient.call() into a Dapr Workflow on Catalyst; the workflow logic in
		     ParallelizationlWorkflow.java is unchanged Spring AI. -->
		<dependency>
			<groupId>io.diagrid</groupId>
			<artifactId>diagrid-spring-ai-starter</artifactId>
			<version>${diagrid-spring-ai.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<dependencyManagement>
		<dependencies>
			<!-- Align Netty across Dapr's grpc-netty (4.1) and Spring Boot 4 (4.2); the mixed
			     state otherwise breaks at runtime. Declared first so it wins over the BOMs below. -->
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-bom</artifactId>
				<version>4.2.12.Final</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>io.dapr</groupId>
				<artifactId>dapr-sdk-bom</artifactId>
				<version>${dapr.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<repositories>
		<repository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
		<repository>
			<id>spring-snapshots</id>
			<name>Spring Snapshots</name>
			<url>https://repo.spring.io/snapshot</url>
			<releases>
				<enabled>false</enabled>
			</releases>
		</repository>
		<repository>
			<id>central-portal-snapshots</id>
			<name>Central Portal Snapshots</name>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
			<releases>
				<enabled>false</enabled>
			</releases>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
		</repository>
	</repositories>

</project>
